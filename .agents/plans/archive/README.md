# Archived Plans

This directory stores closed implementation plans that no longer need active execution or release-preparation updates.

Preserve each archived plan's `Plan-ID`, filename, status, close reason, and validation history. Archived plan filenames must start with the stable `PLAN-<short-kebab-slug>` ID.

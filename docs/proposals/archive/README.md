# Archived Proposals

This directory stores fully retired proposal documents.

Move a proposal here only after it has no `open` tracker rows and its entry has been moved from `Active Proposals` to `Completed Proposals` in [../README.md](../README.md).

Preserve proposal IDs, proposal filenames, and finding IDs when archiving.

# Validation Report: v0.1.0-beta.3

- Date: 2026-06-04
- Source ref: local `main` release preparation for `v0.1.0-beta.3`
- Artifact: local pre-tag candidate
  `build/distributions/ai-commit-all-0.1.0-beta.2+11.85dc987c1b.dirty.zip`.
  The tag-triggered GitHub Release workflow must rebuild from the annotated
  `v0.1.0-beta.3` tag and attach the clean tagged distribution ZIP.
- IDE matrix: automated release verifier target `IU-2026.1.1`, `PY-2026.1.1`,
  `WS-2026.1.1`
- Automated gates:
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/generate-intellij-platform-change-notes.ps1 -Check` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/generate-intellij-platform-description.ps1 -Check` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/validate-docs.ps1` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/ai/validate-agent-artifacts.ps1` passed.
  - `.\gradlew.bat spotlessCheck verifyDetektBaseline detekt test jacocoTestReport verifyJacocoCoverageReport verifyPluginStructure buildPlugin` passed; `test` reported 413 passing and 1 pending.
  - `.\gradlew.bat verifyPlugin "-PpluginVerifierIdeVersions=IU-2026.1.1,PY-2026.1.1,WS-2026.1.1" -PpluginPublishChannels=default` passed on direct retry. IntelliJ IDEA, PyCharm, and WebStorm were reported `Compatible` with 2 experimental API usages for `git4idea.push.GitPushListener.onCompleted`.
- Completed-work archive sweep:
  - No active tasks, plans, or proposals were ready to archive. `T-VAL-024`
    remains open for the manual Marketplace release validation matrix.
- Transient validation notes:
  - The first `verifyPlugin` run failed before a compatibility result because
    the WebStorm verifier worker ended with `java.nio.file.ClosedFileSystemException`
    while loading bundled IDE plugins. The immediate direct retry completed
    successfully for IDEA, PyCharm, and WebStorm.
- Manual checklist:
  - Not executed in this GitHub prerelease tag preparation pass.
- Evidence paths:
  - `build/reports/pluginVerifier/IU-261.23567.138`
  - `build/reports/pluginVerifier/PY-261.23567.174`
  - `build/reports/pluginVerifier/WS-261.23567.141`
  - `build/reports/tests/test`
  - `build/reports/jacoco/test/html`
  - `build/reports/detekt`
  - `build/distributions/ai-commit-all-0.1.0-beta.2+11.85dc987c1b.dirty.zip`
- Skipped checks:
  - Manual release validation matrix remains tracked by `T-VAL-024` and is
    required before Marketplace release-readiness claims.
  - The gated JetBrains Marketplace `Release` workflow was not dispatched, so
    signing and JetBrains Marketplace publication were not attempted.
- Release-readiness conclusion:
  - Automated local validation supports committing release preparation and
    pushing the `v0.1.0-beta.3` prerelease tag. This report does not support
    Marketplace publication until the manual release validation matrix is
    completed and the gated release workflow passes from `main` at the
    annotated tag.

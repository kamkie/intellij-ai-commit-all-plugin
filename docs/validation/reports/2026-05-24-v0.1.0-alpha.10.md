# Validation Report: v0.1.0-alpha.10

- Date: 2026-05-24
- Source ref: `main` release preparation for `v0.1.0-alpha.10`
- Artifact: local pre-tag candidate `build/distributions/ai-commit-all-v0.1.0-alpha.9-135-g24c9153742.dirty.zip`
- IDE matrix: automated release verifier target `IU-2026.1.1`, `PY-2026.1.1`, `WS-2026.1.1`
- Automated gates:
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/validate-docs.ps1` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/ai/validate-agent-artifacts.ps1` passed.
  - `.\gradlew.bat spotlessCheck detekt` passed after refactoring the staged-file status predicates to satisfy Detekt `ComplexCondition`.
  - `.\gradlew.bat test --tests "pl.devopssolutions.aicommitall.vcs.GitStageSelectionItemsTest"` passed.
  - `.\gradlew.bat verifyPlugin "-PpluginVerifierIdeVersions=IU-2026.1.1,PY-2026.1.1,WS-2026.1.1" -PpluginPublishChannels=default` passed after a transient verifier worker retry.
  - `.\gradlew.bat spotlessCheck detekt test jacocoTestReport verifyJacocoCoverageReport verifyPluginStructure buildPlugin verifyPlugin "-PpluginVerifierIdeVersions=IU-2026.1.1,PY-2026.1.1,WS-2026.1.1" -PpluginPublishChannels=default` passed.
- Transient validation notes:
  - The first full Gradle release gate stopped in `detekt` on `GitStageSelectionItems.kt` with a `ComplexCondition` finding; the code was refactored without changing staging behavior.
  - A later full Gradle release gate passed through docs, formatting, Detekt, tests, coverage, structure, and packaging, then failed inside the JetBrains Plugin Verifier worker for `PY-261.23567.174` with `java.nio.file.ClosedFileSystemException`. The immediate direct verifier rerun and the following full release gate both reported `Compatible` for IDEA, PyCharm, and WebStorm.
- Manual checklist:
  - Not executed in this release-tag preparation pass.
- Evidence paths:
  - `build/reports/pluginVerifier/IU-261.23567.138`
  - `build/reports/pluginVerifier/PY-261.23567.174`
  - `build/reports/pluginVerifier/WS-261.23567.141`
  - `build/reports/tests/test`
  - `build/reports/jacoco/test/html`
  - `build/reports/detekt`
- Skipped checks:
  - Manual release validation matrix remains tracked by `T-VAL-024` and is required before Marketplace release-readiness claims.
  - The GitHub Actions `Release` workflow was not dispatched, so signing and JetBrains Marketplace publication were not attempted.
- Release-readiness conclusion:
  - Automated local validation supports preparing and pushing the `v0.1.0-alpha.10` prerelease tag. This report does not support Marketplace publication until the manual release validation matrix is completed and the gated release workflow passes from `main` at the annotated tag.

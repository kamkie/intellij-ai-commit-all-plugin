# Validation Report: v0.1.0-beta.9

- Date: 2026-07-10
- Source ref: local `main` release preparation for `v0.1.0-beta.9`, based on
  green commit `94b0c83e97d589bfc82c50a04c4823d75995a13f`.
- Artifact: remote-first pre-tag candidate. The tag-triggered GitHub Release
  workflow must rebuild from the annotated `v0.1.0-beta.9` tag and attach the
  clean tagged distribution ZIP.
- IDE matrix: GitHub Plugin Verifier targets `IU-2026.1.1`, `PY-2026.1.1`, and
  `WS-2026.1.1`; the tag-triggered Release Matrix UI workflow covers the
  configured IDEA full lane and PyCharm/WebStorm smoke lanes.
- Automated gates:
  - Main-branch `CI`, `Security`, `Plugin Verifier`, `CodeQL`, and
    `Dependency Submission` workflows passed for commit
    `94b0c83e97d589bfc82c50a04c4823d75995a13f` before release preparation.
  - Main `CI` run `29110215801` passed its build, test, coverage, packaging,
    and 13-test PyCharm release-matrix smoke lane.
  - `scripts/run-remote-first-beta-validation.ps1 -Tag v0.1.0-beta.9`
    passed on the release-preparation commit.
  - Local Gradle, tests, coverage, Plugin Verifier, and release-matrix UI gates
    were intentionally deferred to GitHub under the documented remote-first
    beta path.
- Completed-work archive sweep:
  - `T-VAL-024` remains open intentionally for the full manual Marketplace
    publication validation matrix and is not archive-ready.
  - No active or completed plans were waiting to archive.
  - No active or completed proposals were waiting to archive.
- Manual checklist:
  - Not executed in this GitHub prerelease tag preparation pass.
- Transient validation notes:
  - The preceding `main` candidate failed the staging-enabled release-matrix
    commit scenario twice at its 60-second Git outcome bound while all other
    smoke scenarios passed. The harness now allows 120 seconds only for that
    staging-enabled scenario; the exact follow-up commit passed the full
    GitHub UI coverage lane.
- Evidence paths:
  - GitHub Actions main `CI` run `29110215801` for commit
    `94b0c83e97d589bfc82c50a04c4823d75995a13f`.
  - `build/github-release-notes-v0.1.0-beta.9.md`.
- Skipped checks:
  - Local Gradle, test, coverage, Plugin Verifier, and release-matrix UI gates
    were deferred to the main-push and tag-triggered GitHub workflows because
    the release-preparation diff is limited to release metadata,
    documentation, generated Marketplace change notes, and this report.
  - The manual release validation matrix remains tracked by `T-VAL-024` and is
    required before Marketplace release-readiness claims.
  - The gated JetBrains Marketplace `Release` workflow was not dispatched, so
    signing and JetBrains Marketplace publication were not attempted.
- Release-readiness conclusion:
  - Main CI plus remote-first local validation support pushing the
    `v0.1.0-beta.9` prerelease tag. Final release readiness depends on the
    post-push watcher confirming the main push workflows, tag-triggered GitHub
    Release and Release Matrix UI workflows, and the GitHub Release ZIP asset.
    This report does not support Marketplace publication.

# Validation Report: v0.1.0-beta.4

- Date: 2026-06-12
- Source ref: local `main` release preparation for `v0.1.0-beta.4`
- Artifact: local pre-tag candidate
  `build/distributions/ai-commit-all-0.1.0-beta.3+14.e105906270.dirty.zip`.
  The tag-triggered GitHub Release workflow must rebuild from the annotated
  `v0.1.0-beta.4` tag and attach the clean tagged distribution ZIP.
- IDE matrix: automated release verifier target `IU-2026.1.1`, `PY-2026.1.1`,
  `WS-2026.1.1`
- Automated gates (via `scripts/run-local-prerelease-validation.ps1`):
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/generate-intellij-platform-change-notes.ps1 -Check` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/generate-intellij-platform-description.ps1 -Check` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/validate-docs.ps1` passed.
  - `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/ai/validate-agent-artifacts.ps1` passed.
  - Non-verifier Gradle gates passed; `test` executed 431 tests with 1
    pre-existing skip.
  - Plugin Verifier ran separately per IDE: IntelliJ IDEA, PyCharm, and
    WebStorm at `2026.1.1` each reported `Compatible` with the 2 known
    experimental API usages for `git4idea.push.GitPushListener.onCompleted`.
- Completed-work archive sweep:
  - `PLAN-workflow-stop-feedback-and-push-alignment` closed with
    `Close-Reason: Released` and moved to `.agents/plans/archive/`.
  - `T-BUG-018` archived to `TASKS_ARCHIVE.md` with red/green validator repro
    evidence. `T-BUG-017` (conditional staging fallback hardening) and
    `T-VAL-024` (manual Marketplace matrix) remain open intentionally.
  - No proposals were active.
- Transient validation notes:
  - The first `run-local-prerelease-validation.ps1` invocation failed in the
    documentation gate because the release checklist linked this report before
    the file existed; the report was created and the full script re-run passed
    end to end with no Gradle or verifier retries needed.
- Manual checklist:
  - Not executed in this GitHub prerelease tag preparation pass.
- Evidence paths:
  - `build/reports/pluginVerifier-local-prerelease/IU-2026.1.1`
  - `build/reports/pluginVerifier-local-prerelease/PY-2026.1.1`
  - `build/reports/pluginVerifier-local-prerelease/WS-2026.1.1`
  - `build/reports/tests/test`
  - `build/reports/jacoco/test/html`
  - `build/reports/detekt`
  - `build/distributions/ai-commit-all-0.1.0-beta.3+14.e105906270.dirty.zip`
- Skipped checks:
  - Manual release validation matrix remains tracked by `T-VAL-024` and is
    required before Marketplace release-readiness claims.
  - The gated JetBrains Marketplace `Release` workflow was not dispatched, so
    signing and JetBrains Marketplace publication were not attempted.
- Release-readiness conclusion:
  - Automated local validation supports committing release preparation and
    pushing the `v0.1.0-beta.4` prerelease tag. This report does not support
    Marketplace publication until the manual release validation matrix is
    completed and the gated release workflow passes from `main` at the
    annotated tag.
